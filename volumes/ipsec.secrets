# /etc/ipsec.secrets - secrets for IKE/IPsec authentication
# Replace the placeholder values with the actual peer identifier and pre-shared key (PSK).

%any <remote-ip> : PSK "change-me-to-a-strong-secret"
